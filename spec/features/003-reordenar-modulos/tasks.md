# Tasks — Reordenar Módulos por Relación

- [ ] Reordenar tarjetas del Dashboard en `src/App.tsx` (Infraestructura → Talento → Oferta → Admin)
- [ ] Reordenar sidebar nav en `src/App.tsx` (mismo orden)
- [ ] Reordenar `PERMISSIONS_BY_MODULE` en `src/modules/index.ts`
- [ ] `npm run lint` — sin errores
- [ ] Commit + deploy sync + push
